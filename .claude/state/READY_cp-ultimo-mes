OS-CP-ULTIMO-MES-01 — Contas a Pagar abre no ultimo mes COM DADOS, nao no mes do calendario
Branch: feature/cp-ultimo-mes
Commit: 1bd6374 (+ commit final de DIARIO/flag)

SINTOMA (diretor):
- Em setembro, com o fechamento de agosto ainda em andamento (setembro nao
  importado), a Central Financeira -> Contas a Pagar abria EM BRANCO. O operador
  tinha que clicar repetidamente em "Carregar mes anterior" ate achar os dados.

CAUSA RAIZ:
- gerenciador_contas_pagar_desktop/code.html, inscreverContasPagar(): a janela
  de carga server-side (auditoria 2026-06-18) era ancorada em
  _cpMesCorrenteRange() -> new Date(). Mes do calendario, nao dado existente.
- O MESMO default aparecia em mais dois pontos: limparTodosFiltros() e o ramo
  "Periodo vazio" de btn-filtros-cp-aplicar. Corrigir so a abertura deixaria
  "Limpar filtros" devolvendo o operador a tela branca.

PREMISSA DO DIRETOR CORRIGIDA (reportada antes de implementar):
- "O Faturamento ja faz isso, replicar o padrao" — o Faturamento NAO tem essa
  logica. contas_a_receber_desktop/code.html:4319 faz
  onSnapshot(collection(db,"Lancamentos")) sem where/orderBy/limit: carrega a
  colecao inteira. Nunca abre em branco porque mostra tudo, sempre. Copiar isso
  para o CP reintroduziria a regressao documentada em code.html:1140
  ("onSnapshot SEM limite carregava TODA a base historica -> travava o
  browser", 10k+ docs). Decisao: manter a janela, trocar so a ANCORA.

ARMADILHA DE DADO (moldou o desenho):
- Nao se pode usar max(data_vencimento): a recorrencia projeta lancamentos ate
  120 meses no futuro (_cpProjetarRecorrencia, code.html:4549; teto padrao =
  Dez do ano ou data_fim_contrato). O topo absoluto da colecao cairia em
  Dez/2026 ou alem — trocaria uma tela branca por outra. Teto duro = fim do mes
  corrente.

CORRECAO (escopo exato da OS — 1 arquivo, sem tocar calculo/valores):
- _cpDescobrirMesPadrao() (code.html:1203): varredura descendente a partir do
  fim do mes corrente, em paginas escalonadas [50,250,500,500], para no PRIMEIRO
  lancamento nao-projetado. Range + orderBy no MESMO campo data_vencimento —
  sem indice composto novo. Falha de leitura ou base vazia -> fallback para o
  comportamento anterior (mes corrente), tela nunca quebra.
- _cpEhProjecao() (code.html:1192): clone de recorrencia NAO ancora a abertura
  (decisao do diretor 2026-09-08). Discriminante persistido:
  origem in {etl_projetado_fixo, edit_projetado_recorrente} ou
  match_tipo == 'projetado'. Doc legado sem esses campos conta como REAL.
- _cpJanelaPadraoAtual() (code.html:1240): "Limpar filtros" e "Aplicar com
  Periodo vazio" voltam para o mes resolvido na abertura, nao para o calendario.
- inscreverContasPagar() (code.html:1398): async, resolve a ancora antes de
  assinar o listener.

BUG ADICIONAL ENCONTRADO E CORRIGIDO (item 2 da OS, autorizado pelo diretor):
- _cpRestaurarEstadoFiltros() (code.html:1697) restaurava o Periodo salvo em
  sessionStorage como filtro CLIENT-SIDE sem re-sincronizar a janela
  SERVER-SIDE. Periodo Jul/2026 restaurado + janela no mes default = intersecao
  vazia: tela branca E o botao "Carregar mes anterior" aparentemente morto (ele
  trazia Ago do servidor, mas o filtro de Jul descartava tudo). Agora a janela
  passa a ser o proprio Periodo restaurado, re-assinada fora do callback do
  snapshot ativo.
- O botao em si NUNCA esteve quebrado: recua o piso 1 mes e re-assina o
  listener corretamente. Diagnostico reportado ao diretor antes de mexer.
- Decisao do diretor 2026-09-08: MANTER o botao (navegacao rapida mes a mes,
  complementar ao filtro de periodo).

GATES:
- Testador-auditor: APROVADO. 19 casos no emulador (Firestore + firestore.rules
  de producao), executando o CODIGO-FONTE REAL extraido do HTML por indice de
  texto (_cpMesCorrenteRange..._cpFmtJanelaLabel) e instanciado com new
  Function — nao uma reimplementacao no teste, para nao haver drift.
  Abertura (10/10):
  · setembro vazio -> abre Ago/2026 (nao branco, nao Dez) ......... OK
  · setembro so com projecao -> ignora e abre Ago .................. OK
  · setembro importado -> abre Set (comportamento anterior) ........ OK
  · doc legado sem campo origem -> conta como real ................. OK
  · 120 projecoes em Set (estoura a 1a pagina de 50) -> acha Ago ... OK
  · base vazia -> fallback mes corrente ............................ OK
  · virada de ano (hoje Jan/2027, dados Dez/2026) .................. OK
  · Fev/2027 -> range fecha em 28 (ultimo dia real) ................ OK
  · perfil consulta (read-only) -> query de abertura permitida ..... OK
  · usuario sem o menu -> permission-denied, fallback sem quebrar .. OK
  Navegacao (9/9):
  · 1o clique Jul (piso), 2o Jun, virada Jan/2026 -> Dez/2025 ...... OK
  · janela vazia ("") parte do mes PADRAO, nao do calendario ....... OK
  · janela Ago=3 docs, apos 1 clique=6, apos 2 cliques=8 ........... OK
  · Periodo restaurado vira a janela -> 3 docs de julho ............ OK
- Sintaxe: code.html parseia limpo como ESM (bloco module, node --check).
- firestore.rules NAO tocado -> gate do emulador de regras nao se aplica
  (DoD do CLAUDE.md). Confirmado que a query nova passa nas regras existentes
  (allow read de /ContasAPagar por hasMenu, sem restricao de query).
- Nenhum indice composto novo em firestore.indexes.json.
- Check anti-segredo: limpo (scripts/check-secrets.cjs).
- Validacao visual do diretor: 2026-09-08, no emulador local (hosting :5000)
  com cenario semeado (Jun/Jul/Ago importados, Setembro sem importacao, 16
  recorrencias projetadas em Set-Dez). Confirmou que a tela abre no ultimo mes
  com dados importados, conforme pedido, e autorizou o deploy.

DESVIO REPORTADO AO DIRETOR (escopo estrito):
- Alem dos pontos listados na OS, foi alterado o texto-placeholder estatico do
  rodape da tabela: "Exibindo: mes corrente" -> "Carregando..." (code.html:350).
  A frase antiga descrevia justamente o comportamento que a OS elimina. E texto
  estatico, sobrescrito no primeiro snapshot por _cpFmtJanelaLabel().

CUSTO / TRADE-OFF ASSUMIDO:
- A abertura passa a fazer uma leitura extra de ate 50 documentos para descobrir
  a ancora (no caso comum para no primeiro doc). Se um mes acumular mais de 50
  projecoes, escala para paginas maiores; testado com 120. Em troca, o operador
  deixa de fazer N cliques manuais toda vez que abre a tela fora do fechamento.

PENDENCIAS NAO BLOQUEANTES (fora de escopo, OS propria se o diretor quiser):
- Enquanto a ancora resolve (1 roundtrip), o estado vazio "Nenhum lancamento
  encontrado" fica visivel por alguns centesimos a mais que antes. Nao ha
  skeleton/spinner nessa fase — ja era assim para o primeiro snapshot.
- scripts/check-syntax.cjs segue quebrado (pre-existente, gate cego): grava
  bloco <script type="module"> com extensao .cjs e node --check rejeita
  qualquer `import`. A verificacao de sintaxe desta OS foi feita com extracao
  para .mjs. Ja registrado em OS anteriores.
- contas_a_receber_desktop (Faturamento) carrega a colecao Lancamentos INTEIRA
  sem janela nem limite. Nao e o escopo desta OS e nao foi tocado, mas e a
  mesma classe de problema de performance que motivou a janela do CP em
  2026-06-18. Merece OS propria antes que a base cresca mais.
